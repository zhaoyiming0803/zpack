(function(modules) {
	var installedModules = {};

	function __zpack_require__(moduleId) {
		if(installedModules[moduleId]) {
			return installedModules[moduleId].exports;
		}
		
		var module = installedModules[moduleId] = {
			i: moduleId,
			l: false,
			exports: {}
		};

		modules[moduleId].call(module.exports, module, module.exports, __zpack_require__);
		module.l = true;
		return module.exports;
	}

	return __zpack_require__(__zpack_require__.s = "<%-entryId%>");
})({
		<%for (let key in modules) {%>
			"<%-key%>": (function(module, exports, __zpack_require__) {
				eval(`<%-modules[key]%>`);
			}),
		<%}%>
});